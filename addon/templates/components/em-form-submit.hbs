{{#if form.isHorizontal}}
    <div class={{horiClass}}>
        <button class={{classes}} type={{type}} disabled={{disabled}}>{{text}}</button>
    </div>
{{else}}
    <button class={{classes}} type={{type}} disabled={{disabled}}>{{text}}</button>
{{/if}}
