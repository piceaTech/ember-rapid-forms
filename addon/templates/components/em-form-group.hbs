{{#if wrapperClass}}
    <div class={{wrapperClass}}>
      {{#unless hasBlock}}
        {{form-group label=label yieldInLabel=yieldInLabel
          labelWrapperClass=labelWrapperClass labelClass=labelClass
          help=help shouldShowErrors=shouldShowErrors
          cid=cid validationIcons=validationIcons mainComponent=this}}
      {{else}}
        {{yield}}
      {{/unless}}
    </div>
{{else}}
  {{#unless hasBlock}}
    {{form-group label=label yieldInLabel=yieldInLabel
      labelWrapperClass=labelWrapperClass labelClass=labelClass
      help=help shouldShowErrors=shouldShowErrors
      cid=cid validationIcons=validationIcons mainComponent=this}}
  {{else}}
    {{yield}}
  {{/unless}}
{{/if}}
